+ drzewo
+ jeden plik
+po kliknięciu na dany bąbel drzewa pokazać tabelkę zawierającą (njabliższe) rozgałęzienie jego sekwencji
+ automatyczny dobór treshold - pierwsza odległość, która przekracza średnią * MNOŻNIK (kolejny parametr, np. 1,1) odległość między compatibility dla danego consensusu
	- dodatkowy parametr stopu (np. 0.999) - jeśli automatycznie dobrany treshold wyjdzie powyżej tej wartości, to już nie rodzielać tej grupy na drobniejsze
+ czy klikanie w node ma rzeczywiście wyświetlać ten podział pod nim?
+ czy zostawić kolorki w tabelce?


todo
- różnicowanie rozmiarów węzłów w zależności od liczności sekwencji w nich
- dołożenie do obecnych liści ich dzieci (czyli pojedynczych sekwencji) i umożliwienie przełączenia do widoku uproszczonego (bez tych dzieci)
- linia odzwierciedlająca wartość suwaka w tle drzewa
- większy mnożnik a znajdowanie treshold - dla 1.3 nie znajduje
- coś jeszcze miało być wyświetlane w nodach?
- repróbowanie w ramach podziału węzła - zrobić bool na to

- zmiana w algorytmie - to chyba jest zrobione, prawda??:
to nie:
    (- po wyznaczeniu 1. consensusu - jego próg dokładamy do następnego wyliczonego consensususu (jeśli jest mniejszy niż najmniejsze comp. z nowowyznaczonego consensusu), jeśli nie - nic się nie zmienia
    - po wyznaczeniu consensusów przelecieć przez wszystkie sekwencje z danego węzła i sprawdzić, czy nie wolałaby trafić do innego consensususu.
    - i tak aż do końca wyznaczania consensusów z danego węzła (czyli jest opcja dołożyć ten pierwszy wyznaczony próg, jesli jest mniejszy niż wszystkie comp. z nowowyznaczonego)
    )
    JEDNAK TAK: (
    - po wyznaczeniu 1. consensusu c1 i progu t1 - liczymy kolejny consensus c2 dla reszty sekwencji. Jeśli t1 będzie mniejsze niż wszystkie compatibility c2, to zostawiamy ją jako osobny węzeł i idziemy do następnej iteracji. Jeśli nie, to wyznaczamy próg t2 (bez dokładania t1 do compatibility c2) i dzielimy grupę. Przy każdej następnej grupce jako wyznacznik tego czy dzielić ją, czy nie, bierzemy min(t1, t2,...)
    - parametr bool, czy chcemy po wyznaczeniu consensusów przelecieć przez wszystkie sekwencje z danego węzła i sprawdzić, czy nie wolałaby trafić do innego consensususu)

10.01 16:30